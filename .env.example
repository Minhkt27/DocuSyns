# Copy this file to .env and fill in real values. .env is gitignored and must never be committed.
# JWT signing secret - use a long random string (64+ bytes recommended). Generate with:
#   openssl rand -base64 64 | tr -d '\n'
JWT_SECRET=

# PostgreSQL password used by both the postgres container and the backend datasource.
DB_PASSWORD=
